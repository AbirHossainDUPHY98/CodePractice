# Use 'Distributed' to run embarassingly parallel tasks on multiple processes. 
#
# Joblib (Python) → parallelizes Python functions across CPU cores (or processes) .Example: Monte Carlo simulations, independent data processing tasks.(Multi-mach  ine tasks with MPI(Limited))
# Distributed (Julia) → runs tasks across multiple Julia processes, which can be   on the same machine (cores) or even across multiple machines.
